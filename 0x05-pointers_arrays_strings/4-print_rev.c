#include "main.h"
/**
 * print_rev - prints a string
 * @str: a pointer to a string
 * Return: Always 0
 */
void print_rev(char *s)
{
int i;
int j;
s[i] = '\0';
for (j < i; s(j); j--)
{
_putchar (c[j]);
}
_putchar ('\n');
}
