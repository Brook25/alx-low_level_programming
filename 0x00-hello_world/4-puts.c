#include <stdio.h>
#include <stdlib.h>
#include <string.h>

/**
 * main - Entry point
 *
 * return: Always 0 (Success)
 */
int main(void)
{
	    char str1[70];

	        strcpy(str1, "\"Programming is like building a multilingual puzzle");

		    puts(str1);

		        return (0);
}

